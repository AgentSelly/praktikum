# Praktikum

Bitte führe folgenden Code in der Konsole auf unserer Startseite ein:

```
console.log(document.querySelector('[name=theme-color]').getAttribute('content'));
```

Das Ergebnis welches zurückgegeben wird, kannst du dann im Bewerbungsformular in das entsprechende Feld eintragen.
